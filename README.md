📌 | Fundamentos e Preparação do Ambiente

Compreender a diferença entre linguagem SQL e linguagens de programação, configurar o ambiente de desenvolvimento (Workbench ou PGAdmin) e revisar os tipos de dados do projeto.

Elaborar e executar comandos de inserção de dados (INSERT) e consulta (SELECT) no banco de dados do projeto, garantindo coerência com o modelo lógico e os tipos de dados.

Aprofundar o uso da DML com comandos de UPDATE e DELETE, garantindo a integridade dos dados e respeitando as restrições impostas pelas chaves e relacionamentos.

Entregas:

Link funcional e público do repositório GitHub com os scripts SQL, instruções de execução, README.md completo.

Seguem alguns dos scripts que devem constar no repositório que serão imprescindíveis para sua avaliação:

Script SQL com comandos de INSERT para povoar as tabelas principais;
Script SQL com duas a cinco consultas usando SELECT (com WHERE, ORDER BY, LIMIT, JOIN etc.);
Script com ao menos três comandos de UPDATE e três de DELETE com condições.# Atividade4
